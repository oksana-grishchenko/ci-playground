name: CI-API
on:
  push:
    paths-ignore:
      - 'ui'
    branches:
      - main


jobs:
  check:
    name: Check-API
    runs-on: ubuntu-latest

    steps:
      - name: Print
        run: echo "Check-API"


